#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    float a, b, c;
    cout << "Enter the side of triangle :: ";
    cin >> a >> b >> c;

    // if (a == b && b == c)
    // {
    //     cout << "Equilateral triangle." << endl;
    // }
    // else if (a == b || b == c || a == c)
    // {
    //     cout << "Isosceles triangle." << endl;
    // }
    // else
    // {
    //     cout << "Scalen triangle" << endl;
    // }

    // check right angle isosceles triangle
    // if (a == b || b == c || a == c)
    // {
    //     if (pow(a, 2) == pow(b, 2) + pow(c, 2) || pow(b, 2) == pow(a, 2) + pow(c, 2) || pow(c, 2) == pow(a, 2) + pow(b, 2))
    //     {
    //         cout << "isosceles triangle." << endl;
    //     }
    // }

    return 0;
}