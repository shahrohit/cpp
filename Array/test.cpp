#include <iostream>
#include <vector>
using namespace std;
int main()
{
    // vector<int> arr{1, 0, 1, 2, 0, 0, 0, 2, 2, 1};
    // vector<int>::iterator iter;

    // int zero=0,one=0;
    // for (iter = arr.begin(); iter != arr.end(); ++iter)
    // {
    //    for
    // }
    int arr[] = {1, 0, 1, 2, 0, 0, 0, 2, 2, 1};
    // vector<int> v=arr;

    return 0;
}